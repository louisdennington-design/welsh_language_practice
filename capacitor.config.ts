import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.welshvocab.practice',
  appName: 'Welsh Vocabulary Practice',
  webDir: 'out'
};

export default config;
